server {
    listen 80;
    server_name dashboard.cairohere.com;

    root /dist/browser/index.html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    error_page 404 /index.html;
}
